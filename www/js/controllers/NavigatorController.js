(function() {
    'use strict';
    var module = angular.module('Navigator', []);

    module.controller('NavigatorController', function($scope, $http) {

        ons.ready(function() {
           
           /* window.myNav.on('prepush', function(event) {
                var page = event.currentPage; // Get current page object
                if (needsCancel) {
                    event.cancel(); // Cancel operation
                }
            });*/
        });
    });

})();
